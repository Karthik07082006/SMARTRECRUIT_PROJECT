server:
  port: ${API_PORT:4000}
spring:
  datasource:
    url: ${DATABASE_URL:jdbc:h2:mem:smartrecruit;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE}
    username: ${DATABASE_USERNAME:sa}
    password: ${DATABASE_PASSWORD:}
  sql:
    init:
      mode: always
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
  mail:
    host: ${SMTP_HOST:}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USER:}
    password: ${SMTP_PASSWORD:}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: ${SMTP_SECURE:false}
smartrecruit:
  jwt-secret: ${JWT_SECRET:smartrecruit-local-secret-change-me}
  client-origin: ${CLIENT_ORIGIN:http://localhost:5173}
  upload-dir: ${UPLOAD_DIR:uploads}
  mail-from: ${SMTP_FROM:}
